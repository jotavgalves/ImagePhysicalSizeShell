# Imaging module

This module contains Phase 1 physical-size calculation and metadata scanning code.

Installation remains disabled. The code is intended for `ipsdiag inspect` and unit tests until the native shell handler is implemented.
